using System.Windows.Controls;

namespace OptiPaie.Desktop.Views
{
    /// <summary>DAC recap tab — read-only, with copy buttons on every figure.</summary>
    public partial class CnasDacView : UserControl
    {
        public CnasDacView()
        {
            InitializeComponent();
        }
    }
}
