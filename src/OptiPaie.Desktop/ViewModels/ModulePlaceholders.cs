namespace OptiPaie.Desktop.ViewModels
{
    // All modules are now implemented as first-class view models
    // (HomeViewModel, EmployeesViewModel, CompaniesViewModel, PayrollViewModel,
    // ArchiveViewModel, SettingsViewModel). This file intentionally holds no types.
}
