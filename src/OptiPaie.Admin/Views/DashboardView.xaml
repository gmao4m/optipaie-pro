<UserControl x:Class="OptiPaie.Admin.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Tableau de bord" Style="{StaticResource H1}" />

        <ItemsControl Grid.Row="1" ItemsSource="{Binding Cards}" Margin="0,18,0,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate><UniformGrid Columns="6" /></ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}" Padding="16" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="{Binding Label}" Style="{StaticResource Caption}" TextWrapping="Wrap" />
                            <TextBlock Text="{Binding Value}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="2" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,8,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Activité récente" Style="{StaticResource H2}" Margin="16,14" />
                    <DataGrid ItemsSource="{Binding Recent}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Date" Binding="{Binding CreatedDisplay}" Width="130" />
                            <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="*" />
                            <DataGridTextColumn Header="Client" Binding="{Binding CompanyName}" Width="150" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,0,0,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Dernières mises à jour" Style="{StaticResource H2}" Margin="16,14" />
                    <DataGrid ItemsSource="{Binding LatestUpdates}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="120" />
                            <DataGridTextColumn Header="Type" Binding="{Binding MandatoryDisplay}" Width="130" />
                            <DataGridTextColumn Header="" Binding="{Binding LatestDisplay}" Width="90" />
                            <DataGridTextColumn Header="Publiée" Binding="{Binding PublishedDisplay}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
